{
  "name": "muk-performance-research-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free federated project portal for the Performance Research Lab at MUK@OWA.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=22" }
}
